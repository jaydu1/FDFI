"""
Setup script for FDFI (backward compatibility).

For modern installation, use pyproject.toml with pip:
    pip install -e .
"""

from setuptools import setup

# Configuration is in pyproject.toml
setup()
